<form method="post" action="{{ path('app_embarcacion_delete', {'id': embarcacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ embarcacion.id) }}">
    <button class="btn">Delete</button>
</form>
